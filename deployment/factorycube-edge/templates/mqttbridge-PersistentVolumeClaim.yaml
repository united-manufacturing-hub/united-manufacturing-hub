
{{- if .Values.mqttbridge.enabled -}}

kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: {{ include "factorycube-core.fullname" . }}-mqttbridge-claim
  labels:
    {{- include "factorycube-core.labels.mqttbridge" . | nindent 4 }}
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.mqttbridge.storageRequest }}

{{- end }}
