---
{{if .Values._000_commonConfig.datasources.sensorconnect.testMode}}

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{include "united-manufacturing-hub.fullname" .}}-sensorconnect-test
data:
  {{- range $key, $value := .Values._000_commonConfig.datasources.sensorconnect.testData}}
  {{ $key }}: {{ $value | quote }}
  {{- end}}

{{end}}