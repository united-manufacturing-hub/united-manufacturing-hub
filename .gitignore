# Git ignore for go (tests, coverage, etc.)
*.test
*.cover
*.out
*.diff

.docker-cache
.idea

# .DS_Store from macos
.DS_Store

# Claude artifacts
compass_artifact*

# Test results
*.result.txt

.claude/settings.local.json

# Prometheus data directory (contains large binary files)
prometheus/data/
prometheus-backup.tar.gz
