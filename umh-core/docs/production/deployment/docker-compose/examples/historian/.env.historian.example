# ==============================================================================
# Historian Addon Environment Variables
# ==============================================================================
# Copy this file to ../../.env (append to your core .env file)
# Or create a separate .env.historian file and use: --env-file .env.historian
#
# These variables are ONLY needed if you're using the historian addon

# ==============================================================================
# REQUIRED: TimescaleDB Credentials
# ==============================================================================
# Database superuser credentials
# SECURITY: Change POSTGRES_PASSWORD to a strong random password for production
# Generate: openssl rand -base64 32
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=umh_v2

# ==============================================================================
# OPTIONAL: Grafana Admin Credentials
# ==============================================================================
# Change these on first login or set custom values
GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=admin

# ==============================================================================
# OPTIONAL: TimescaleDB Tuning
# ==============================================================================
# Adjust based on your server resources
# TS_TUNE_MEMORY: RAM to allocate (e.g., 4GB, 8GB, 16GB)
# TS_TUNE_NUM_CPUS: CPU cores to use
TS_TUNE_MEMORY=4GB
TS_TUNE_NUM_CPUS=2

# ==============================================================================
# Security Notes
# ==============================================================================
# 1. Use strong passwords for POSTGRES_PASSWORD (NOT "changeme")
# 2. Do NOT expose TimescaleDB port (5432) to public networks
# 3. Change Grafana admin password immediately after first login
# 4. Rotate database credentials quarterly
# 5. Use SSH tunnel or VPN for remote database access
