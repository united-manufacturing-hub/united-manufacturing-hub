agent:
  metricsPort: 8080
# The contract name does not follow _<model>_<version>. Shipped in
# examples/example-config-streamprocessor-templated.yaml, so it occurs.
dataModels:
  - name: pump
    version:
      v1:
        structure:
          pressure:
            _payloadshape: timeseries-number
dataContracts:
  - name: _pump-contract_v1
    model:
      name: pump
      version: v1
