{{- if .Values.cameraconnect.enabled -}}

apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "factorycube-edge.fullname" . }}-cameraconnect-genTLProducer
binaryData:
    files.zip: 
        {{ .Files.Get "genTLProducer/files.zip" | b64enc }}

{{- end }}
