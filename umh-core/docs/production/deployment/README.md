# Deployment

This section covers how to deploy umh-core in production environments.

## Deployment Options

umh-core is distributed as a container image. We provide instructions for using [Docker](docker/README.md) and [Docker Compose](docker-compose/README.md) to deploy it.
