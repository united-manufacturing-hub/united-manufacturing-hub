# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib

# Test binary, built with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

.env
.dccache

/**/persistentData/*

# do not include automatically created PDFs 
docs/**/*.pdf
golang/test/mqtt-to-postgresql/config/certificates/*