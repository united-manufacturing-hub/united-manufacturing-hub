apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "factorycube-server.fullname" . }}-test-connection"
  labels:
    {{- include "factorycube-server.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "factorycube-server.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
