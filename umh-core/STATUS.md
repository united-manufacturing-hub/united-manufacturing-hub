- [x] **Rung 2 — saturation omitted at construction (F17 saturation half)** · tdd-commit + conductor closure · commit `4ad93eba4` (amended) · **75/75 cpuhealth + 106/106 diagnosis**.
  - What shipped: `cpuTable` appends `saturationSignal` ONLY when `cores > 0`, mirroring the existing `limit-saturation` quota>0 guard. A box whose core count was never readable declares no saturation row at all. **No `HasCoreCount` capability** — the core count is a construction-time fact, not a per-tick capability. The two F2 guard specs were REWRITTEN to assert the row's absence (the load-bearing acceptance; a green suite without that rewrite fails the rung).
  - **The wrong-but-green trap, this time with teeth.** When cpuTable omits saturation, the old F2 specs still passed unedited — `Engine.Reduction` on an omitted signal returns the zero `Reduced` (StateAbsent, 0.0), exactly what they asserted. So GREEN was reachable with zero test changes and the specs' intent (signal exists, extractors guard it) silently false. Enforced: the F2 specs must assert `hasSignal(cpuTable(0,2.0), sigSaturation) == false`. This is the rung-1 lesson generalized (fix the goal, not the form).
  - Conductor closure (fix subagent + independent reviewer, both passing):
    1. Removed a false-coverage host_headroom spec whose message-field assertions passed identically under both designs (only `hasSignal` absence discriminated).
    2. Pinned the omission through the EXPORTED `Table(0,2.0)` route — the exact route a worker polls — closing the "worker polls vs engine keys" drift gap.
    3. Collapsed a triplicated doc sentence; reframed the Extract-arm guards as defense-in-depth (unreachable through the gated append, not the gate).
  - Recording gate: unaffected — `RunSuite` only ever `cores=4`, so saturation never omitted there; `unsupportedSaturation == Ready` at cores=4 holds. Pressure (rung 1) untouched.
  - ⚠️ DEFERRED TO P3 (not a rung-2 defect, reviewer concurred): the one-shot core count has no per-tick recovery — a transiently-unreadable core count at the single build instant permanently grounds saturation. Tracked `cpu_health_nopsi_regression` systemic. P3 landmine; P2 has no worker wiring yet.
