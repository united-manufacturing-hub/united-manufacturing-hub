{{- if .Values.services.nodered.enabled -}}

apiVersion: v1
kind: ConfigMap
metadata:  
  name: {{ include "factorycube-unified.fullname" . }}-nodered-flows
data:  
  nodered-flows: {{ .Values.nodered.flows | toPrettyJson }}

{{- end }}
