# Copyright 2023 UMH Systems GmbH
#
# Licensed under the Apache License, Version 2.0 (the "License");
# you may not use this file except in compliance with the License.
# You may obtain a copy of the License at
#
#     http://www.apache.org/licenses/LICENSE-2.0
#
# Unless required by applicable law or agreed to in writing, software
# distributed under the License is distributed on an "AS IS" BASIS,
# WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
# See the License for the specific language governing permissions and
# limitations under the License.

name: Build Docker Images

on:
  push:
    paths:
      - 'deployment/**'
      - '!deployment/*.ps1'
      - 'golang/**'
      - '.github/workflows/build-docker-images-ghcr.yml'

jobs:
  barcodereader:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./deployment/barcodereader/Dockerfile
          push: true
          tags: ghcr.io/unitedmanufacturinghub/barcodereader:${GITHUB_REF##refs/*/}


  factoryinput:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./deployment/factoryinput/Dockerfile
          push: true
          tags: ghcr.io/unitedmanufacturinghub/factoryinput:${GITHUB_REF##refs/*/}

  factoryinsight:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./deployment/factoryinsight/Dockerfile
          push: true
          tags: ghcr.io/unitedmanufacturinghub/factoryinsight:${GITHUB_REF##refs/*/}

  grafana-proxy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./deployment/grafana-proxy/Dockerfile
          push: true
          tags: ghcr.io/unitedmanufacturinghub/grafana-proxy:${GITHUB_REF##refs/*/}

  grafana-umh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./deployment/grafana-umh/Dockerfile
          push: true
          tags: ghcr.io/unitedmanufacturinghub/grafana-umh:${GITHUB_REF##refs/*/}

  kafka-bridge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./deployment/kafka-bridge/Dockerfile
          push: true
          tags: ghcr.io/unitedmanufacturinghub/kafka-bridge:${GITHUB_REF##refs/*/}

  kafka-debug:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./deployment/kafka-debug/Dockerfile
          push: true
          tags: ghcr.io/unitedmanufacturinghub/kafka-debug:${GITHUB_REF##refs/*/}

  kafka-init:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./deployment/kafka-init/Dockerfile
          push: true
          tags: ghcr.io/unitedmanufacturinghub/kafka-init:${GITHUB_REF##refs/*/}

  kafka-state-detector:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./deployment/kafka-state-detector/Dockerfile
          push: true
          tags: ghcr.io/unitedmanufacturinghub/kafka-state-detector:${GITHUB_REF##refs/*/}

  kafka-to-blob:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./deployment/kafka-to-blob/Dockerfile
          push: true
          tags: ghcr.io/unitedmanufacturinghub/kafka-to-blob:${GITHUB_REF##refs/*/}

  kafka-to-postgresql:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./deployment/kafka-to-postgresql/Dockerfile
          push: true
          tags: ghcr.io/unitedmanufacturinghub/kafka-to-postgresql:${GITHUB_REF##refs/*/}

  mqtt-bridge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./deployment/mqtt-bridge/Dockerfile
          push: true
          tags: ghcr.io/unitedmanufacturinghub/mqtt-bridge:${GITHUB_REF##refs/*/}

  mqtt-kafka-bridge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./deployment/mqtt-kafka-bridge/Dockerfile
          push: true
          tags: ghcr.io/unitedmanufacturinghub/mqtt-kafka-bridge:${GITHUB_REF##refs/*/}

  sensorconnect:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Login to GitHub Container Registry
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Build and Push Docker Image
        uses: docker/build-push-action@v2
        with:
          context: .
          file: ./deployment/sensorconnect/Dockerfile
          push: true
          tags: ghcr.io/unitedmanufacturinghub/sensorconnect:${GITHUB_REF##refs/*/}

